<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Financial Details - Financial Decision Assistant</title>
    <link rel="icon" type="image/jpeg" href="download.jpeg">
    <link rel="stylesheet" href="styles.css">
    <style>
        body {
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            background-color: #7C93C3;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* Full viewport height */
        }

        nav {
            background-color: #1E2A5E;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed; /* Fixed position to stay at the top */
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        nav li {
            margin: 0 10px;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }

        nav a:hover {
            text-decoration: underline;
        }

        .main-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 60px); /* Adjust height to avoid being hidden by navbar */
            margin-top: 60px; /* Push content below navbar */
            padding: 20px;
            box-sizing: border-box;
        }

        .financial-container {
            background-color: rgba(255, 255, 255, 0.7);
            border: 1px solid #1E2A5E;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 600px; /* Increased width for more horizontal space */
            text-align: center;
        }

        .financial-container h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #1E2A5E;
        }

        .financial-container input[type="text"],
        .financial-container input[type="number"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            transition: box-shadow 0.3s ease;
        }

        /* Two input fields side by side */
        .form-group-horizontal {
            display: flex;
            justify-content: space-between;
            gap: 10px; /* Gap between the inputs */
        }

        .form-group-horizontal input {
            width: 48%; /* Each input takes up 48% of the width */
        }

        /* Hover effect for inputs */
        .financial-container input[type="text"]:hover,
        .financial-container input[type="number"]:hover {
            box-shadow: 0 0 10px #1E2A5E;
        }

        .financial-container button {
            width: 60%;
            padding: 10px;
            margin: 10px;
            background-color: #1E2A5E;
            color: white;
            border: 2px solid #1E2A5E;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        .financial-container button:hover {
            background-color: #55679C;
        }

        @media (max-width: 768px) {
            .financial-container {
                width: 90%; /* Adjust for smaller screens */
            }

            .form-group-horizontal {
                flex-direction: column;
            }

            .form-group-horizontal input {
                width: 100%; /* Full width in mobile view */
            }
        }
    </style>
</head>
<body>

    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/about-us">About Us</a></li>
            <li><a href="/contact">Contact</a></li>
        </ul>
    </nav>

    <div class="main-container">
        <div class="financial-container">
            <h1>Financial Details</h1>
            <form id="financial-form" method="POST" action="/financial-details">
                <!-- Grouped inputs: Income and Assets -->
                <div class="form-group-horizontal">
                    <input type="number" id="income" name="income" placeholder="Income (₹)" required>
                    <input type="number" id="total_assets" name="total_assets" placeholder="Assets (₹)" required>
                </div>

                <!-- Grouped inputs: Expenses and Debts -->
                <div class="form-group-horizontal">
                    <input type="number" id="expenses" name="expenses" placeholder="Expenses (₹)" required>
                    <input type="number" id="debt" name="debt" placeholder="Debts (₹)" required>
                </div>

                <!-- Grouped inputs: Liabilities and Savings -->
                <div class="form-group-horizontal">
                    <input type="number" id="total_liabilities" name="total_liabilities" placeholder="Liabilities (₹)" required>
                    <input type="number" id="savings" name="savings" placeholder="Savings (₹)" required>
                </div>

                <!-- Profit (alone) -->
                <input type="number" id="profit" name="profit" placeholder="Profit (₹)" required>

                <button type="submit">Update Financial Info</button>
                <button type="button" onclick="window.location.href='/dashboard'">Back to Dashboard</button>
            </form>
        </div>
    </div>

</body>
</html>
