<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Edit Profile - Financial Decision Assistant</title>
    <link rel="icon" type="image/jpeg" href="download.jpeg">
    <link rel="stylesheet" href="styles.css">
    <style>
        nav {
            background-color: #1E2A5E;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        nav li {
            margin: 0 10px;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }

        nav a:hover {
            text-decoration: underline;
        }

        body {
            background-color: #7C93C3;
            font-family: 'Times New Roman', 'Arial Narrow', Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding-top: 60px; /* Space for fixed navbar */
        }

        .profile-container {
            background-color: rgba(255, 255, 255, 0.7);
            border: 1px solid #1E2A5E;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }

        .profile-container h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }

        .profile-container input[type="text"],
        .profile-container input[type="email"],
        .profile-container select {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            transition: box-shadow 0.3s ease;
        }

        .profile-container input[type="text"]:hover,
        .profile-container input[type="email"]:hover,
        .profile-container select:hover {
            box-shadow: 0 0 10px #1E2A5E;
        }

        .form-group-horizontal {
            display: flex;
            justify-content: space-between;
        }

        .form-group-horizontal input {
            width: 48%;
        }

        .button-group {
            display: flex;
            justify-content: space-between;
            margin-top: 20px; /* Add space above the button group */
        }

        .profile-container button {
            width: 48%; /* Adjusted to fit side by side */
            padding: 10px;
            background-color: #1E2A5E;
            border: 2px solid #1E2A5E;
            color: white;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        .profile-container button:hover {
            background-color: #55679C;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/about-us">About Us</a></li>
            <li><a href="/contact">Contact</a></li>
        </ul>
    </nav>

    <!-- Edit Profile Form -->
    <div class="profile-container">
        <h1>Edit Profile</h1>
        <form id="edit-profile-form" method="POST" action="/edit-profile">
            <!-- Full Name -->
            <div class="form-group-horizontal">
                <input type="text" id="first-name" name="first_name" placeholder="First Name" required>
                <input type="text" id="last-name" name="last_name" placeholder="Last Name">
            </div>

            <!-- Country Code and Contact Number -->
            <div class="form-group-horizontal">
                <select id="isd-code" name="isd_code" required>
                    <option value="">ISD</option>
                    <option value="+91">+91 India</option>
                    <option value="+1">+1 USA</option>
                    <option value="+44">+44 UK</option>
                    <option value="+61">+61 Australia</option>
                </select> 
                <input type="text" id="contact" name="contact" placeholder="Contact Number" maxlength="10" required>
            </div>

            <!-- Email -->
            <input type="email" id="email" name="email" placeholder="Email" required>

            <div class="button-group">
                <button type="submit">Update Profile</button>
                <button type="button" onclick="window.location.href='/dashboard'">Back to Dashboard</button>
            </div>
        </form>
    </div>
</body>
</html>
